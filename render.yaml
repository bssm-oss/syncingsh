services:
  - type: web
    name: syncingsh-signaling
    runtime: node
    plan: free
    rootDir: signaling
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: PORT
        value: 10000
