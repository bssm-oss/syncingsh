services:
  signaling:
    build: .
    restart: unless-stopped
    ports:
      - "4444:4444"
    environment:
      - PORT=4444
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4444/health"]
      interval: 30s
      timeout: 5s
      retries: 3
